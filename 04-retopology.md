# Ретопология

Ретопология - это процесс перестроения топологии меша, чтобы получить чистую и удобную для редактирования сетку.

Она используется когда:

* Модель создана через скульптинг
* Модель получена после Boolean операций
* Геометрия содержит слишком много полигонов
* Сетка имеет хаотичную структуру

Цель ретопологии - получить сетку, которая:

* Легко редактируется
* Предсказуемо сглаживается
* Корректно деформируется
* Имеет минимальное количество полигонов

## Основы топологии и структура меша

Топология - это структура соединения вершин, ребер и граней

Важно не только количество полигонов, но и как они соединены между собой

Две модели могут иметь одинаковую форму, но:

* Одна будет легко редактироваться
* Другая будет ломаться при любом изменении

### High Poly vs Low Poly

#### High Poly

High Poly - модель с большим количеством полигонов

Используется для:

* Скульптинга
* Детализированных рендеров
* Создания normal maps

В такой модели как правило большое количество вершин и ее сложно редактировать

### Low Poly

Low Poly - упрощенная версия модели. Такая модель описывает форму минимальным количеством полигонов

Используется:

* В играх
* В рендерах реального времени
* Для оптимизации сцены

### Quad сетка

В большинстве случаев предпочтительна сетка из четырехугольников (quads)

Quad - это грань с 4 вершинами

TODO: пример quad'а

Такая сетка образует регулярную решетку

#### Почему quad лучше tris

GPU всегда рисует треугольнки (иного не умеет). Любая модель перед рендерингом будет разбита на отдельные треугольники.

Тем не менее, на практике моделирование почти всегда выполняется в квадратах. Причина заключается в удобстве моделирования и особенности работы инструментов (например subdivision)

Реальные формы чаще имеют параллельные линии, т.к. большинство объектов состоит из:

* Параллельных поверхностей
* Прямых линий

У треугольника нет параллельных линий и им неудобно описывать форму большинства объектов, а Quad сетка естественно описывает такие формы. Так же Quad сетка формирует логические петли ребер (edge loops), с помощью которых потом легко деформировать и менять форму объекта

Subdivision Surface работает лучше всего с quad сеткой. При использовании треугольников появляются:

* Артефакты
* Неправильные складки
* Поломанный shading

#### Но GPU все равно использует треугольники

Важно понимать:

> ⚠️ Любая 3D модель в конечном итоге триангулируется

Это происходит:

* При экспорте (при выборе соответствующей опции)
* При рендере
* В игровом движке (если модель заранее не триангулирована)

GPU работает только с треугольниками

Но:

* Quad -> триангулируется предсказуемо
* N-gon -> триангулируется случайно

Экспортированная модель с n-gon'ами может триангулироваться в раных программах по-разному. По этому если n-gone выглядит удачно в Blender - это не обязательно будет так же в Substance Painter или UE

### Треугольники (Triangles, tris)

Треугольник - грань из трех вершин

При моделировании они допустимы, но их стараются избегать, чтобы не разрывать разрывать edge flow. Наличие треугольников так же добавляет артефакты при использовании subdivision

### N-gons

N-gon - грань с 5 и более сторонами. В некоторых определениях - с 4 и более (т.е. quad тоже относят к n-gone)

Они могут вызвать:

* Проблемы сглаживания
* Артефакты при subdivision
* Хаотичную триангуляцию

Использовать можно:

* На плоских поверхностях
* В местах без деформации

Сам по себе n-gone не является чем-то очень плохим, т.к. он все равно будет приведен к треугольникам. На плоских поверхностях не имеет разницы, как именно n-gon будет триангулирован

### Edge Flow и Edge Loops

Edge Flow - направление ребер в модели

Хороший edge flow:

* Повторяет форму объекта
* Образует логические петли
* Облегчает деформацию

Пример:

* Вокруг глаз персонажа
* Вокруг суставов
* Вокруг отверстий

Edge Loop - кольцо ребер, проходящее через quad сетку

Выделить loop можно `Alt + Click`

### Poles

Pole - вершина, к которой подключено не 4 ребра

* 3-edge pole
* 5-edge pole
* 6-edge pole

Poles используются для перенаправления edge loops и изменения плотности сетки

### Равномерность плотности сетки

Хорошая сетка имеет примерно одинаковую плотность полигонов

Плохо:

* Одна часть очень плотная
* Другая почти пустая

### Единый mesh vs раздельные объекты

В один объект объединяют:

* Непрерывные поверхности
* Органические формы
* Части одной структуры

Например:

* Тело персонажа
* Мягкие формы

Разделять стоит:

* Механические детали
* Независимые элементы
* Разные материалы

Например:

* Болты
* Панели
* Крышки

### Проверка сетки

#### Wireframe

Показывает структуру сетки

#### Wireframe Overlay

Позволяет видеть сетку поверх модели

Помогает анализировать:

* Плотность сетки
* Edge flow

#### Проверка Subdivision

Добавление модификатора Subdivision Surface показывает:

* Как сглаживается модель
* Где появляются артефакты

## Проблемы плохой топологии и их исправление

### N-gons

Проблема: грань имеет слишком много сторон

Почему плохо:

* Ломается сглаживание
* Unpredictable triangulation

Исправление

* Разделить на quads
* Добавить edge loops

### Ломаный edge flow

Проблема: ребра идут хаотично

Почему плохо:

* Сложное редактирование
* Неправильная деформация

Исправление:

* Перестроить loops
* Удалить лишние ребра

### Лишние loops

Проблема: слишком много edge loops

Почему плохо:

* лишние полигоны
* сложная сетка

Исправление: `Dissolve Edge Loop`

### Дубли вершин

Проблема: несколько вершин находятся в одном месте

Почему плохо:

* Ломается shading
* Появляются щели

Исправление: `M -> By Distance`

### Перекрестная геометрия

Проблема: полигоны пересекаются

Почему плохо:

* Артефакты
* Неправильный shading

Исправление:

* Удалить пересечения
* Перестроить геометрию

### Boolean мусор

После boolean часто появляется:

* N-gons
* Хаотичные полигоны
* Плохая топология

Исправляется через:

* Ручную ретопологию
* Удаление лишних ребер

## Сломанный shading

Проблема: поверхность выглядит неровной или имеет странные затенения

Причина: неправильные нормали

Исправление: `Shift + N`

## Инструменты анализа

### Face Orientation

Показывает направление полигонов, где красный - обратная сторона

### X-Ray режим

Позволяет видеть геометрию сквозь объект

Комбинация для включения: `Alt + Z`

### Проверка сгибом

Небольшая деформация модели позволяет увидеть:

* Складки
* Искажения
* Наложение геометрии

### Subdivision проверка

Добавление Subdivision Surface показывает:

* Проблемы топологии
* Неправильные loops

### Анализ меша (Mesh Analysis)

Blender имеет инструменты анализа геометрии

Mesh Analysis позволяет проверить:

* Толщину
* Кривизну
* Искажения
* Нависания (актуально для 3D печати)

---

## Базовая ретопология в Blender

Ретопология может выполняться:

* Вручную
* Автоматически
* Через модификаторы

### Snap to Face

Позволяет размещать вершины на поверхности другого объекта

Настройки: `Snapping -> Face`

### Shrinkwrap Modifier

Модификатор притягивает сетку к поверхности другого объекта

### Poly Build

Инструмент для ручного построения quad сетки

### Loop Cut

Добавляет edge loop

Комбинация в Edit Mode: `Ctrl + R`

### Merge

Объединяет вершины

Комбинация: `M`

### Dissolve

Удаляет элементы без разрушения поверхности

Комбинация в Edit Mode: `X -> Dissolve`

### QuadriFlow Remesh

QuadriFlow - инструмент автоматической ретопологии

Он создает:

* Quad сетку
* Равномерную плотность

Используется когда:

* Модель слишком сложная
* Нужна быстрая базовая топология

Для моделей, которые потом планируется анимировать, не подходит (только ручная ретопология)
